/*
 *  key  constant
 */

/**
 * key prefix
 */
const KEY_PREFIX = 'smart_admin_';
/**
 * localStorageKey collection
 */
export default {
  // user permission
  USER_POINTS: `${KEY_PREFIX}user_points`,
  // tag list
  USER_TAG_NAV: `${KEY_PREFIX}user_tag_nav`,
  // app config info
  APP_CONFIG: `${KEY_PREFIX}app_config`,
};
